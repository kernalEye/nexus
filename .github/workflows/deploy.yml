name: Deploy

on:
push:
branches:
- main

jobs:
deploy:
runs-on: self-hosted

```
steps:
  - name: Deploy Application
    run: |
      cd /opt/kernaleye
      git pull origin main
      docker compose up -d --build
```
